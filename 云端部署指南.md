# 云端部署指南（Render 免费云主机）

> 目标：不依赖协会电脑常开，把"物料管家"免费托管到云上，得到一个**永久的 https:// 网址**。
> 之后把网址发给社员，手机流量、宿舍、家里都能打开使用，数据实时同步。
> 全程约 15 分钟，免费。

---

## 一、原理一句话

| 角色 | 位置 | 说明 |
|------|------|------|
| 网页（前端） | 也放在 Render 上 | 打开网址就是系统，不用再传文件 |
| 同步服务器 | Render 上 | 所有人的数据汇总到它这里 |
| 每人浏览器 | 各自手机/电脑 | 离线也能记，联网自动补传 |

Render（render.com）是国外老牌免费托管平台，免费档每月 750 小时（够一个服务全天跑），
自带 https 证书，不需要备案。

---

## 二、准备：注册 GitHub 账号（约 5 分钟）

Render 从 GitHub 仓库读取代码，所以先要有一个 GitHub 账号。

1. 打开 https://github.com → 点右上角 **Sign up** → 填邮箱、密码注册。
2. 注册后登录，点右上角 **+** → **New repository** 新建仓库：
   - Repository name：填 `hwms`（或任意英文名）
   - 选择 **Private**（私有，别人看不到）
   - 其他默认，点 **Create repository**。

## 三、把系统文件上传到 GitHub（约 5 分钟）

1. 在刚建好的仓库页面，找到 **uploading an existing file**（上传现有文件）链接，点它。
2. **把 hwms 文件夹里的全部内容**（不是文件夹本身，是里面的东西）拖进上传框：
   ```
   index.html、style.css、manifest.json、sw.js、js/ 文件夹、icons/ 文件夹、
   assets/ 文件夹、server/ 文件夹、云端部署指南.md 等
   ```
   > 注意：是"文件夹里面的内容"直接拖进去，仓库根目录要能直接看到 index.html 和 server 文件夹。
3. 页面底部点 **Commit changes**（提交），等上传完成。
4. 以后系统升级了，同样方法进入仓库 → **Add file → Upload files**，覆盖上传即可。

## 四、在 Render 上创建服务（约 5 分钟）

1. 打开 https://render.com → 右上角 **Get Started** → 选 **Sign in with GitHub**，
   用刚才的 GitHub 账号直接登录（不用再注册）。
2. 登录后点 **New +** → 选 **Web Service**。
3. 如果提示授权，选 **Configure** 允许 Render 访问你的 GitHub 仓库，然后选中 `hwms` 仓库。
4. 填写创建表单：
   | 配置项 | 填什么 |
   |--------|--------|
   | Name | `hwms`（网址会变成 https://hwms-xxxx.onrender.com） |
   | Language / Runtime | **Node** |
   | Region | **Singapore**（新加坡，国内访问最快） |
   | Branch | main |
   | Build Command | 留空（或填 `echo ok`） |
   | Start Command | `node server/server.js` ← 照抄 |
   | Instance Type | **Free** |
5. **重要**：往下找到 **Environment Variables**（环境变量），添加一条：
   - Key：`SYNC_KEY`
   - Value：自己编一个够长的密码，例如 `jkb-2026-hwms-secret`（**记住它，第 6 步要用**）

   > 这就是"同步密钥"：没有它，陌生人知道网址也读不了你们的数据。
6. 点最下面的 **Create Web Service / Deploy Web Service**。
7. 等待 1~3 分钟，日志滚动到 `Your service is live` 就成功了。
   页面顶部会显示网址，形如：`https://hwms-a1b2.onrender.com`

**自检**：浏览器打开这个网址，能看到物料管家的登录页，说明部署成功。

## 五、手机电脑如何使用

把网址发给社员即可，例如：`https://hwms-a1b2.onrender.com`
- 首次打开注册账号（第一个注册的账号自动成为管理员）。
- 想像 App 一样用：浏览器菜单 → "添加到主屏幕 / 安装"（参见 README 的 PWA 说明）。

## 六、在系统里填同步配置（约 2 分钟）

部署好后，在**任意一台已登录**的设备上：

1. 打开 **系统设置 → 多端同步**。
2. 同步地址填：`https://hwms-a1b2.onrender.com`（你的网址）
3. 同步密钥填：第 4 步第 5 小步设的 `SYNC_KEY` 值。
4. 开启"启用同步"，点 **测试连接**，提示成功后保存。
5. 第一台设备会自动把本机数据全量上传到云端（补种）；其他设备登录后拉取即可。

> 旧版本没有"同步密钥"输入框？刷新页面（Ctrl+F5 强制刷新）就会加载新版。

---

## 七、免费档的三个特性（都要知道）

### 1. 15 分钟没人用会"睡觉"
免费实例闲置 15 分钟后会休眠，下次访问要等约 1 分钟"唤醒"（页面会转圈，属正常现象）。
- 实在想秒开：注册 https://cron-job.org（免费），新建任务每 10 分钟访问一次你的网址即可保持常醒。

### 2. 重启后服务器磁盘会被清空（有自动自愈，不用慌）
免费档每次重启/重新部署，服务器上的 data.json 会被清空。本系统已内置**数据自愈**：
- 服务器重启后，**任意一个成员**打开系统联网同步时，检测到云端是空库，
  会自动把本机完整数据重新上传，云端瞬间恢复。
- 唯一要求：至少有一名成员的浏览器里保存过数据（正常使用必然满足）。

### 3. 建议每月导出一次 CSV 备份
物料库页 → **导出 Excel（CSV）**，存到协会网盘一份。有备无患。

---

## 八、常见问题

| 问题 | 处理 |
|------|------|
| Render 部署日志报错 `Cannot find module` | Start Command 抄错或仓库根目录没有 server 文件夹，对照第四步检查 |
| 打开网址 502 / Not Found | 部署没完成，等日志出现 live 再刷新；检查 Start Command 是否为 `node server/server.js` |
| 测试连接提示"同步密钥不正确" | Render 环境变量 SYNC_KEY 和系统设置里填的不一致，两边改成一样的 |
| 想改网址前缀 | Render 的 Name 改了重新部署即可（网址会跟着变，记得更新各设备配置） |
| 国内偶尔打不开 | 免费档在境外，个别网络波动属正常，换流量/网络重试；重要数据记得 CSV 备份 |
| 不想用 Render 了 | 数据都在各设备本地，新平台部署好填新地址，数据会自动重新补种过去 |

## 九、如何更新系统版本

1. GitHub 仓库 → **Add file → Upload files** → 拖入新版文件 → Commit changes。
2. Render 检测到仓库更新会**自动重新部署**（约 1 分钟）。
3. 各设备浏览器强制刷新（Ctrl+F5）加载新前端。

---

*指南对应服务器版本：server.js（支持 SYNC_KEY 密钥校验 + 空库自动补种）。*
